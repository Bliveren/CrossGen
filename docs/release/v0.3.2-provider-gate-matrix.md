# CrossGen v0.3.2 Provider Gate Matrix

> Status: template for v0.3.2 release candidates  
> Purpose: prove image task reliability and diagnostics by operation, route, model, timeout, and output.

## Rules

- Do not record API keys or full private Base URLs.
- Record provider kind and route behavior, not a specific aggregator brand.
- Text-to-image success does not prove image-to-image support.
- Image-to-image success does not prove guided-region/mask support.
- User-configured timeout must be honored. If a task times out, record the configured timeout and elapsed seconds.
- A failed gate can pass release only when the product surface shows a correct diagnostic and a usable next action for an unsupported or provider-limited operation.

## Generic Gate Runner

Use the generic provider gate runner for compatible endpoints that expose
chat-completions-style image output. The runner records operation-level evidence
without writing API keys or full private Base URLs to disk.

```bash
CROSSGEN_REAL_PROVIDER_ACCEPT_COST=1 \
CROSSGEN_REAL_PROVIDER_BASE_URL="https://<redacted-compatible-base-url>/v1" \
CROSSGEN_REAL_PROVIDER_API_KEY="<redacted>" \
CROSSGEN_REAL_PROVIDER_GEMINI_API_KEY="<redacted-or-same-key>" \
pnpm verify:real-provider-gate
```

Optional variables:

- `CROSSGEN_REAL_OPENAI_MODEL`: default `gpt-image-2`.
- `CROSSGEN_REAL_GEMINI_MODEL`: default `gemini-3.1-flash-image`.
- `CROSSGEN_REAL_PROVIDER_TIMEOUT_MS`: default `240000`, minimum `30000`.
- `CROSSGEN_REAL_PROVIDER_BASE_URL_TYPE`: default `compatible-gateway`.
- `CROSSGEN_REAL_PROVIDER_OUTPUT_DIR`: default `real-api-artifacts`.
- `CROSSGEN_REAL_PROVIDER_ALLOW_DIAGNOSTIC_FAILURES=1`: only after a
  human reviewer confirms the desktop UI shows an accurate diagnostic and next
  action for provider-limited operations.

The generated `summary.json` is release evidence, but it is not a replacement
for product-owner testing of the exact candidate package.

## Required Fields

For every run, record:

- Date and tester.
- Commit SHA and package artifact identity.
- Platform.
- Provider kind: `openai`, `openai-compatible`, `gemini`, or `custom`.
- Base URL type: official, compatible gateway, local mock, or other redacted category.
- Model id.
- Operation: text-to-image, image-to-image, guided-region edit.
- Route selection: auto, chat-completions, responses, image-api.
- Resolved route.
- Input image count.
- Mask: yes/no.
- Timeout seconds.
- Attempt count.
- Elapsed seconds.
- Result: success, failed as expected, failed unexpectedly.
- Output count and saved asset ids if successful.
- Diagnostic category if failed.
- User-visible next actions if failed.

## Matrix

| Provider kind | Model | Operation | Route selection | Timeout | Required result |
| --- | --- | --- | --- | --- | --- |
| OpenAI-compatible | GPT Image 2 or equivalent | text-to-image | auto | user default | Must produce image or actionable diagnostic |
| OpenAI-compatible | GPT Image 2 or equivalent | image-to-image | auto | user default | Must produce image or actionable route/capability diagnostic |
| OpenAI-compatible | GPT Image 2 or equivalent | image-to-image | chat-completions | user default | Must produce image or actionable route/capability diagnostic |
| OpenAI-compatible | GPT Image 2 or equivalent | guided-region edit | image-api/edit route if available | user default | Must produce image or explicit unsupported route/capability diagnostic |
| Gemini-compatible | Nano Banana/Gemini image model | text-to-image | native/default | user default | Must produce image or actionable diagnostic |
| Gemini-compatible | Nano Banana/Gemini image model | image-to-image | native/default | user default | Must produce image or actionable diagnostic |
| Official OpenAI, if key available | GPT Image 2 | text-to-image | auto | user default | Must produce image |
| Official OpenAI, if key available | GPT Image 2 | image-to-image | auto | user default | Must produce image or official capability diagnostic |
| Official OpenAI, if key available | GPT Image 2 | guided-region edit | official supported route | user default | Must produce image or official capability diagnostic |

## Failure Classification Checklist

Use the normalized category that best matches the user-facing cause:

- `auth`: invalid or missing key.
- `billing`: insufficient balance, quota exhausted, payment required.
- `rate_limit`: provider rate limit or concurrency rejection.
- `timeout`: user timeout reached.
- `safety`: content safety or moderation block.
- `unsupported_capability`: model does not support reference/edit/mask.
- `route_unsupported`: endpoint path or route does not support the operation.
- `provider_empty_output`: provider returned no saveable image, including `data: null`.
- `provider_schema`: provider returned a malformed or incompatible response shape.
- `network`: network or DNS failure.
- `cancelled`: user or worker cancellation.
- `unknown`: only when no clearer category is possible.

## Evidence Template

```markdown
### Gate run: <short title>

- Date:
- Tester:
- Commit:
- Package:
- Platform:
- Provider kind:
- Base URL type:
- Model:
- Operation:
- Route selection:
- Resolved route:
- Input image count:
- Mask:
- Timeout seconds:
- Attempt count:
- Elapsed seconds:
- Result:
- Output count:
- Diagnostic category:
- User-visible next actions:
- Notes:
```
